# dbt packages configuration
# Install packages by running: dbt deps

packages:
  # dbt utilities - useful macros and tests
  - package: dbt-labs/dbt_utils
    version: 1.1.1
  
  # Codegen - helps generate dbt model files
  - package: dbt-labs/codegen
    version: 0.12.1
  
  # Add more packages as needed:
  # - package: calogica/dbt_expectations
  #   version: 0.10.1

